# LampaTrakt — контекст проекта

## Проект

Репозиторий: `KZuev/LampaTrakt` (публичный).
Основной файл: `trakttv.js` — плагин Trakt.tv для медиа-интерфейса Lampa (3.0+, Maker/ContentRows API).
Общение с пользователем — **на русском**.

Плагин добавляет на главную/категории строки Trakt (Up Next, Watchlist, Recommendations, Calendar), полноэкранные каталоги, scrobbling (отметка просмотренного).

## Текущая версия

**v2.9.20** — последний релиз, ждёт проверки.

## История фиксов (v2.9.x)

| Версия | Коммит | Суть |
|--------|--------|------|
| v2.9.6 | `2913abc` | Убран `scroll--nopadding` → фикс прыжка постера при фокусе |
| v2.9.7 | `f1968d2` | Отметка серии на `stop`/`ended` когда `progress >= threshold` |
| v2.9.8 | `4cabff4` | Восстановление `Base.start` внутри `.then()` (не помогло — краш был не там) |
| v2.9.9  | `a0d833f` | **Настоящий фикс краша** `items-line__head` (см. ниже) |
| —       | `e874ccb` | Хуки Agentfy в `.claude/settings.json` |
| v2.9.10 | `5c12bf0` | Фикс залезания постеров под кнопки фильтра/сортировки в хабах (`margin-bottom` + снятие `scroll--nopadding` для watchlist) |
| v2.9.11 | `ff91754` | «Смотрю сериалы»: фильтр скрытых/дропнутых чинился — эндпоинт `/users/me/hidden/*` не существует (404 → пустой набор), исправлен на `/users/hidden/{section}`, добавлена секция `dropped`, убрана несуществующая `watched` |
| v2.9.12 | `1d22068` | (1) Бейдж даты в Истории: правый верхний угол, фиолетовая подложка, «Просмотрено 01 мая 2026»; (2) «Смотрю сериалы» → «Мои сериалы», в меню после «Хочу посмотреть»; (3) «Мои сериалы» — хаб `watchingHub` с табами Активные/Завершённые/Брошенные/Все + счётчики (`Api.watchingCounts`, параметр `watchingFilter` в `Api.watching`) |
| v2.9.13 | `fe95555` | (1) История: серия в названии — «Футурама (S01E04)» (суффикс после `enrichWithTmdbLocale`, иначе затирается); (2) «Мои сериалы»: фикс навигации вверх на табы (`onHead` в `baseComponent` работал только для `watchlist`, добавлен `watching`); (3) уменьшен шрифт табов + ellipsis (`.trakt-watching-hub__tabs`) |
| v2.9.14 | TBD       | (1) «Мои сериалы»: активная вкладка при фокусе не делает текст чёрным (CSS override `.trakt-watching-hub__tabs .simple-button.focus`); (2) Счётчик «Брошенные» — фикс: `droppedSet` проверяется до `hiddenSet` в `watchingCounts`; (3) Прогресс-бар поверх постера в списке «Брошенные» (`renderDroppedProgressBar`, `.trakt-progress-bar`); (4) «Смотреть дальше»: в начало списка добавляются начатые фильмы (`/sync/playback?type=movies`) |
| v2.9.15 | TBD       | «Избранное»: фильтры и сортировка — `collectionHub` (тип/год/жанр/страна/сортировка по добавлению‑названию‑году‑рейтингу‑случайно); `Api.collection` переработан для client-side фильтрации; `baseComponent` limit 500 для `type=collection` |
| v2.9.16 | TBD       | Добавление/удаление из избранного в меню карточки (пункт «В избранное»/«Убрать из избранного» в меню Trakt.TV на карточке) |
| v2.9.17 | TBD       | «Мои сериалы»: новая логика активных/завершённых, кружок остатка, «Бросить/Возобновить» в меню карточки |
| v2.9.18 | `94a5cf3` | Три бага совместного просмотра: (1) `clearAllRowCaches()` при изменении multiwatch; (2) `recommendations()` без `applyMultiwatchFilter`; (3) «Мои сериалы» скрывается при включённом multiwatch, убран неверный `applyMultiwatchFilter` из `watching()` |
| v2.9.19 | `4cf75c1` | Бейджи «хочу посмотреть» и «дата цифрового релиза» перекрашены в фиолетовый Trakt |
| v2.9.20 | `c60172f` | Фиолетовая галочка на сериалах: `completedShows` Set (все вышедшие серии просмотрены через `aired_episodes`); ретроактивное применение при загрузке кэша; `_patchLampaCard` охватывает теперь и сериалы |

## Механика краша v2.9.9 (для понимания)

`prefetchMainRows` сохранял строки в кэш без `displayTitle` → `title: undefined` → Lampa удаляет `.items-line__head` у безымянных линий → `more.onVisible` крашится `null.append` при `total_pages > 1` (watchlist/upnext > 36 элементов), когда live API не успевает к дедлайну и отдаётся stale-кэш.

Три исправления в v2.9.9:
- `prefetchMainRows` — конфиги дополнены `displayTitle`/`component` как в `registerRows`
- `loadRowFromCache` — отбрасывает и чистит записи без `title` (отравленный кэш в localStorage)
- `saveRowToCache` — не сохраняет строки без `title`

## Открытые задачи

- [ ] Подтвердить, что краш `items-line__head` ушёл (v2.9.9) — ждём проверки на Apple TV
- [ ] Подтвердить отметку просмотренной серии и отсутствие ложных отметок (v2.9.7 + `_isPlayerActive`)
- [ ] Проверить Stop-хук Agentfy в конце сессии
- [ ] Подтвердить, что постеры больше не заходят под кнопки фильтра/сортировки (v2.9.10)
- [ ] Подтвердить, что скрытые/дропнутые сериалы исчезли из «Смотрю сериалы» (v2.9.11)
- [ ] Проверить v2.9.12: бейдж даты в Истории, «Мои сериалы» (порядок в меню, табы, счётчики)
- [ ] Проверить v2.9.13: серия в названии в Истории, навигация на табы в «Мои сериалы» на Apple TV, размер шрифта табов
- [ ] Проверить v2.9.14: цвет текста активной вкладки при фокусе, счётчик брошенных, прогресс-бар у брошенных, начатые фильмы в «Смотреть дальше»
- [ ] Проверить v2.9.15: фильтры и сортировка в «Избранное»
- [ ] Проверить v2.9.16: добавление/удаление из избранного через меню карточки
- [ ] Проверить v2.9.17: «Мои сериалы» — новая логика активных/завершённых, кружок остатка, «Бросить/Возобновить» в меню карточки
- [ ] Проверить v2.9.18: multiwatch — row cache очищается немедленно, рекомендации фильтруются, «Мои сериалы» скрывается при включённом multiwatch
- [ ] Проверить v2.9.19: бейджи «хочу посмотреть» и «дата цифрового релиза» — фиолетовые
- [ ] Проверить v2.9.20: галочка у полностью просмотренных сериалов везде на сайте

## Важные детали

**Lampa source:**
- `/tmp/lampa-source` — может отсутствовать в новой сессии, клонировать при надобности
- `cf.lampa.mx` недоступен из облачной сессии (403 `host_not_allowed`) — продовый `app.min.js` не скачать

**Механика Lampa (не трогать):**
- `Layer.need_visible` глобальный; `frameVisible` триггерит `visible` каскадно на все `layer--visible` элементы
- `Line.create()` удаляет `.items-line__head` если `!data.title && !has(Icon)`
- `more.onVisible` делает `find('.items-line__head').append` без проверки null при `total_pages > 1`
- `registerMainScreenAutoLoad` пустой намеренно — scroll-hack удалён (вызывал краши)
- `_isPlayerActive` guard в `processTimelineUpdate` — не трогать (защита от ложных отметок)

**Хуки Agentfy (`.claude/settings.json`):**
- Хуки: `SessionStart`, `PermissionRequest`, `Stop`, `Notification` → POST на `https://webhook.getagentfy.com`
- Токен **не в репо** (репо публичный!) — берётся из env `AGENTFY_TOKEN`, без него `exit 0`
- Ошибки curl глушатся (`|| true`)
- Проверка в новой сессии: `echo ${AGENTFY_TOKEN:+задан}`, `curl -s -o /dev/null -w "%{http_code}" -m 10 https://webhook.getagentfy.com` (ожидается 405)

**Git:**
- PR не создавать без явной просьбы
- Пушить в `origin/main` и синхронизировать сессионную ветку (stop-хук проверяет её):
  `git push origin HEAD:main && git push origin HEAD:SESSION_BRANCH --force`
  где `SESSION_BRANCH` — текущая рабочая ветка (`git branch --show-current`)

## Ветки

- `main` — основная, всегда актуальна
- Рабочие ветки сессий: `claude/simplify-trakt-plugin-3PgnH` (прошлая), `claude/agentfy-hooks-check-cvf7vu` (текущая)
